services:
  db:
    image: timescale/timescaledb:latest-pg16
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: vigamonitor
      POSTGRES_USER: vigamonitor
      POSTGRES_PASSWORD: VigaMonitor2025
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vigamonitor -d vigamonitor"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    environment:
      DATABASE_URL: postgresql://vigamonitor:VigaMonitor2025@db:5432/vigamonitor
      CORS_ORIGINS: "*"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  pgdata: